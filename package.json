{
  "name": "arminiapps",
  "version": "0.1.0",
  "private": true,
  "description": "AR Mini Apps — конструктор AR приложений",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
